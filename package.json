{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "lerna": "^6.0.3"
  },
  "scripts": {
    "lint": "lerna run lint --stream",
    "start": "lerna run start --stream"
  }
}
